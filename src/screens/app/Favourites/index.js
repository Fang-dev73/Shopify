import React from 'react'
import { View, ScrollView, Text } from 'react-native'

const Favourites = () => {
  return (
      <ScrollView>
        <Text>
          Favourites
        </Text>
      </ScrollView>
  )
}

export default Favourites;
