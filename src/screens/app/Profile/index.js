import React from 'react'
import { Text, View, ScrollView } from 'react-native'

const Profile = () => {
  return (
      <ScrollView>
        <Text>Profile</Text>
      </ScrollView>
  )
}

export default Profile;
